# macro.get

[Back to tool index](INDEX.md)

Agent name: `winctl-cmp`

Description: Get a promoted macro manifest by session macro ID or memory item ID.

## Inputs

- `id`: session macro ID or memory item ID.

## Notes

When reading from memory, the memory item must contain a valid macro manifest payload.
