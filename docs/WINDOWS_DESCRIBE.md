# windows.describe

[Back to tool index](INDEX.md)

Agent name: `winctl-mcp`

Description: Describe an existing bound window record by `bound_id`.

## Inputs

- `bound_id`: bound window ID returned by `windows.bind`.

## Notes

This describes the saved binding record. It does not perform focus, input, or capture.
